import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  clearButton: {
    position: 'absolute',
    right: 10,
    width: 18,
    marginTop: 'auto',
    marginBottom: 'auto',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
